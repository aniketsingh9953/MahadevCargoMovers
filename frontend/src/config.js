// config.js
// If you deploy the frontend and backend separately (e.g. frontend on
// Netlify/Vercel and backend on Render), change API_BASE to your backend's
// full URL, e.g. "https://mahadev-cargo-backend.onrender.com/api".
//
// If you deploy them together (the Express server also serves the frontend,
// as set up in server.js), leave this as "/api" — it will just work.

const API_BASE = '/api';
